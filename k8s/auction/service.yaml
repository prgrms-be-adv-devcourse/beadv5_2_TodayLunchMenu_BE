apiVersion: v1
kind: Service
metadata:
  name: auction
  namespace: goods-mall
spec:
  type: NodePort
  selector:
    app: auction
  ports:
    - port: 8090
      targetPort: 8090
      nodePort: 30245
